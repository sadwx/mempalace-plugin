# mempalace local state (generated by mempalace init)
entities.json
mempalace.yaml

# Serena MCP server local state (generated on first use)
.serena/

# Claude Code per-developer local settings (MCP approvals etc.)
.claude/settings.local.json
